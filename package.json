{
  "name": "@litecanvas/plugin-timers",
  "version": "0.8.0",
  "description": "Plugin to help manage timers in litecanvas games",
  "author": "Luiz Bills <luizbills@pm.me>",
  "license": "MIT",
  "homepage": "https://github.com/litecanvas/plugin-timers#readme",
  "bugs": {
    "url": "https://github.com/litecanvas/plugin-timers/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/litecanvas/plugin-timers.git#main"
  },
  "main": "src/index.js",
  "keywords": [
    "litecanvas",
    "timer"
  ],
  "scripts": {
    "dev": "esbuild src/_web.js --bundle --watch --outfile=dist/dist.js --servedir=.",
    "build": "esbuild src/_web.js --bundle --minify --outfile=dist/dist.js",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "peerDependencies": {
    "litecanvas": "*"
  },
  "files": [
    "dist",
    "src"
  ]
}
